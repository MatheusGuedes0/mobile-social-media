<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissões -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mobilesocialmedia"
        tools:targetApi="31">

        <!-- Tela de Login -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Tela Principal -->
        <activity android:name=".MainActivity" />
        <activity android:name=".RankingActivity" />
        <activity android:name=".CriarGrupoActivity" />
        <activity android:name=".SelecionarUsuariosActivity" />
        <activity android:name=".ListaGruposActivity" />
        <activity android:name=".SelecionarUsuariosActivity" />
        <activity android:name=".UsuariosDoGrupoActivity" />


        <!-- Serviço de Atividade Física em segundo plano -->
        <service
            android:name=".service.AtividadeService"
            android:exported="false"
            android:foregroundServiceType="health" />
    </application>

</manifest>
